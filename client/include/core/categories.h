#ifndef GHOSTCLIENT_CATEGORIES_H
#define GHOSTCLIENT_CATEGORIES_H

namespace core {

/// @brief Defines the categories for organizing modules.
/// These directly correspond to the sections in the GUI.
enum class Category {
    Combat,
    Misc,
    Movement,
    Visual,
    Menu,
    Configs
};

} // namespace core

#endif // GHOSTCLIENT_CATEGORIES_H
